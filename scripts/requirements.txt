# ============================================================
# ひだまりこそだち 自動投稿 Python 依存関係
# ------------------------------------------------------------
# インストール:
#   pip install -r scripts/requirements.txt
#   playwright install chromium   # Playwright のブラウザ取得
# ============================================================

python-dotenv>=1.0.0
python-frontmatter>=1.0.0
requests>=2.31.0
playwright>=1.40.0
boto3>=1.34.0
click>=8.1.0
